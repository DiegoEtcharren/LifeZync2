// Import jQuery and expose it globally
import $ from 'jquery';
window.$ = window.jQuery = $;

// Bootstrap (CSS and JS)
import 'bootstrap';

// Owl Carousel: 
import 'owl.carousel';

// Your custom scripts (assumed to be in the same folder)
import './counter.js';
import './custom.js';
import './isotope.min.js';